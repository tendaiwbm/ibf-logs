TABLE_NAME = "AppEvents"

ORDER_BY = " sort by {} {}"

PAGINATION_FILTER = " where TimeGenerated {} {}"

PAGINATION_DIRECTION = {
                        "left": "<",
                        "right": ">"
                       }

FORMAT_QUERY = " |".join

